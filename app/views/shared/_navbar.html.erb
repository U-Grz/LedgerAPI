<%# app/views/shared/_navbar.html.erb %>
<nav class="bg-white shadow-lg">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <!-- Logo -->
      <div class="flex items-center">
        <%= link_to dashboard_path, class: "flex items-center space-x-2" do %>
          <span class="text-2xl">💰</span>
          <span class="text-xl font-bold text-gray-900">Ledger</span>
        <% end %>
      </div>
      
      <!-- Navigation Links -->
      <div class="flex items-center space-x-4">
        <%= link_to "Dashboard", dashboard_path, 
            class: "text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium transition" %>
        
        <%= link_to "Transactions", transactions_path, 
            class: "text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium transition" %>
        
        <!-- Subscription/Upgrade Button -->
        <% if current_user.subscription.present? && current_user.subscribed? %>
          <a href="<%= subscription_path %>" class="flex items-center space-x-1 text-indigo-600 px-3 py-2 rounded-md text-sm font-medium">
            <span>⭐</span>
            <span class="font-bold"><%= current_user.subscription.plan.capitalize %></span>
          </a>
        <% else %>
          <a href="<%= new_subscription_path %>" class="bg-gradient-to-r from-indigo-600 to-purple-600 text-white px-4 py-2 rounded-lg text-sm font-bold hover:from-indigo-700 hover:to-purple-700 transition flex items-center space-x-1">
            <span>✨</span>
            <span>Upgrade to Pro</span>
          </a>
        <% end %>
        
        <!-- User Menu -->
        <div class="border-l border-gray-300 pl-4 ml-4 flex items-center space-x-4">
          <span class="text-gray-700 text-sm">
            <%= current_user.name %>
          </span>
          
          <%= button_to "Logout", logout_path, 
              method: :delete,
              class: "bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg text-sm font-medium transition" %>
        </div>
      </div>
    </div>
  </div>
</nav>